const form=document.getElementById('login-form');const error=document.getElementById('login-error');form.addEventListener('submit',async e=>{e.preventDefault();error.textContent='';const data=Object.fromEntries(new FormData(form).entries());const button=form.querySelector('button');button.disabled=true;button.textContent='Signing in…';try{const r=await fetch('/api/auth/login',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(data)});if(!r.ok){let message='Invalid email or password';try{message=(await r.json()).message||message}catch{}throw new Error(message)}const auth=await r.json();sessionStorage.setItem('operation_user',auth.fullName);sessionStorage.setItem('operation_role',auth.role);location.replace('/');}catch(err){error.textContent=err.message}finally{button.disabled=false;button.textContent='Sign in'}});
const state={dashboard:null,customers:[],assets:[],workOrders:[],deliveries:[],inventory:[],health:null};
const CACHE_KEY='yardflow-dashboard-cache-v2';
const CACHE_TTL=60000;
let searchAbort=null;

const pretty=s=>(s||'').toString().replaceAll('_',' ').replace(/\b\w/g,c=>c.toUpperCase());
const escapeHtml=v=>String(v??'').replace(/[&<>'"]/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;',"'":'&#39;','"':'&quot;'}[c]));
const money=v=>v==null?'—':new Intl.NumberFormat('en-IN',{style:'currency',currency:'INR',maximumFractionDigits:0}).format(v);
const date=v=>v?new Date(v).toLocaleString('en-IN',{dateStyle:'medium',timeStyle:'short'}):'—';
const debounce=(fn,wait=180)=>{let t;return(...args)=>{clearTimeout(t);t=setTimeout(()=>fn(...args),wait)}};
const badgeClass=s=>{if(['COMPLETED','DELIVERED','INSTALLED','READY_FOR_DISPATCH','VERIFIED','UP','CLOSED','AVAILABLE','PAID','APPROVED'].includes(s))return'green';if(['BLOCKED','DELAYED','CANCELLED','DOWN','OPEN','OVERDUE'].includes(s))return'red';if(['IN_PROGRESS','IN_TRANSIT','DISPATCHED','NEAR_DESTINATION'].includes(s))return'blue';if(['QUALITY_CHECK','CUSTOMER_APPROVAL_PENDING','INSPECTION_PENDING','ESTIMATE_PENDING','DEGRADED'].includes(s))return'amber';return'gray'};

async function api(url,options={}){
  const headers={...(options.body?{'Content-Type':'application/json'}:{}),...(options.headers||{})};
  const res=await fetch(url,{credentials:'same-origin',...options,headers});
  if(res.status===401){location.replace('/login.html');throw new Error('Session expired')}
  if(!res.ok){let m='Request failed';try{const b=await res.json();m=b.message||b.error||m}catch{}throw new Error(m)}
  if(res.status===204)return null;
  const type=res.headers.get('content-type')||'';
  return type.includes('application/json')?res.json():res.text();
}

function toast(msg){const el=document.getElementById('toast');el.textContent=msg;el.classList.add('show');clearTimeout(el._timer);el._timer=setTimeout(()=>el.classList.remove('show'),2400)}
function openModal(id){document.getElementById(id)?.classList.add('open');syncSelects()}
function closeModal(id){document.getElementById(id)?.classList.remove('open')}
function showView(id){document.querySelectorAll('.view').forEach(v=>v.classList.remove('active-view'));document.getElementById(id)?.classList.add('active-view');document.querySelectorAll('.nav-item[data-view]').forEach(b=>b.classList.toggle('active',b.dataset.view===id));document.getElementById('page-title').textContent=id==='dashboard'?'Operations Overview':id==='system'?'System Health':id==='search'?'Global Search':pretty(id);history.replaceState(null,'',`#${id}`);if(id==='system')loadHealth()}
function openQuickCreate(){openModal('work-modal')}

document.querySelectorAll('.nav-item[data-view]').forEach(b=>b.addEventListener('click',()=>showView(b.dataset.view)));
document.querySelectorAll('.modal').forEach(m=>m.addEventListener('click',e=>{if(e.target===m)closeModal(m.id)}));
document.addEventListener('keydown',e=>{if(e.key==='Escape')document.querySelectorAll('.modal.open').forEach(m=>closeModal(m.id));if((e.ctrlKey||e.metaKey)&&e.key.toLowerCase()==='k'){e.preventDefault();showView('search');document.getElementById('global-search')?.focus()}});

function showSkeletons(){const box='<div class="skeleton" style="height:72px"></div>';document.getElementById('kpis').innerHTML=box.repeat(5);document.getElementById('active-work').innerHTML='<div class="skeleton" style="height:180px"></div>';document.getElementById('alerts').innerHTML='<div class="skeleton" style="height:140px"></div>';document.getElementById('delivery-preview').innerHTML='<div class="skeleton" style="height:160px"></div>';document.getElementById('low-stock').innerHTML='<div class="skeleton" style="height:140px"></div>'}
function loadCache(){try{const c=JSON.parse(sessionStorage.getItem(CACHE_KEY)||'null');if(!c||Date.now()-c.savedAt>CACHE_TTL)return false;Object.assign(state,c.data);renderAll();syncSelects();return true}catch{return false}}
function saveCache(){try{sessionStorage.setItem(CACHE_KEY,JSON.stringify({savedAt:Date.now(),data:{dashboard:state.dashboard,customers:state.customers,assets:state.assets,workOrders:state.workOrders,deliveries:state.deliveries,inventory:state.inventory}}))}catch{}}

async function refreshAll(manual=false){
  const btn=document.getElementById('refresh-btn');if(btn&&manual){btn.disabled=true;btn.textContent='↻ Refreshing…'}
  try{
    const[d,c,a,w,del,i]=await Promise.all([api('/api/dashboard'),api('/api/customers'),api('/api/assets'),api('/api/work-orders'),api('/api/deliveries'),api('/api/inventory')]);
    Object.assign(state,{dashboard:d,customers:c,assets:a,workOrders:w,deliveries:del,inventory:i});renderAll();syncSelects();saveCache();loadHealth(true);if(manual)toast('Operations refreshed');
  }catch(e){toast(e.message)}finally{if(btn&&manual){btn.disabled=false;btn.textContent='↻ Refresh'}}
}
function renderAll(){renderDashboard();renderWorkOrders();renderAssets();renderDeliveries();renderInventory();renderCustomers()}

function renderDashboard(){const d=state.dashboard;if(!d)return;const k=[['Customers',d.customers,'◎'],['Assets',d.assets,'▣'],['Active Work',d.activeWorkOrders,'⌁'],['In Transit',d.deliveriesInTransit,'➜'],['Low Stock',d.lowStockItems,'!']];document.getElementById('kpis').innerHTML=k.map(([label,value,icon])=>`<div class="kpi"><div class="kpi-top"><span class="label">${label}</span><span class="kpi-icon">${icon}</span></div><div class="value">${value??0}</div><div class="label">Live operational count</div></div>`).join('');const active=state.workOrders.filter(w=>!['COMPLETED','CANCELLED'].includes(w.status)).slice(0,6);document.getElementById('active-work').innerHTML=active.length?active.map(w=>`<div class="work-row"><div><span class="code">${escapeHtml(w.orderNumber)}</span><span class="sub">${escapeHtml(w.assetCode)} · ${escapeHtml(w.customerName)}</span></div><div><span class="badge ${badgeClass(w.status)}">${pretty(w.status)}</span><span class="sub">${pretty(w.workType)}</span></div><div><span class="sub">${w.progressPercent??0}% complete</span><div class="progress"><span style="width:${Math.max(0,Math.min(100,w.progressPercent??0))}%"></span></div></div><button class="mini-btn" onclick="editWork(${w.id})">Update</button></div>`).join(''):'<div class="empty">No active work orders. The yard is clear.</div>';const alerts=d.alerts||[];document.getElementById('alerts').innerHTML=alerts.length?alerts.map(a=>`<div class="alert ${a.severity==='HIGH'?'high':''}"><div class="alert-dot"></div><div><strong>${pretty(a.type)} · ${escapeHtml(a.reference)}</strong><p>${escapeHtml(a.message)}</p></div></div>`).join(''):'<div class="empty">No exceptions require attention.</div>';document.getElementById('delivery-preview').innerHTML=deliveryTable(state.deliveries.slice(0,5));const low=state.inventory.filter(i=>i.lowStock).slice(0,6);document.getElementById('low-stock').innerHTML=low.length?low.map(i=>`<div class="low-stock-item"><div><strong>${escapeHtml(i.name)}</strong><span class="sub">${escapeHtml(i.sku)}</span></div><span class="badge red">${i.quantityOnHand} ${escapeHtml(i.unit||'')}</span></div>`).join(''):'<div class="empty">Inventory levels are healthy.</div>'}

function renderWorkOrders(){const q=(document.getElementById('work-search')?.value||'').toLowerCase(),f=document.getElementById('work-filter')?.value||'';const rows=state.workOrders.filter(w=>(!f||w.status===f)&&[w.orderNumber,w.assetCode,w.customerName,w.assignedTeam].join(' ').toLowerCase().includes(q));document.getElementById('work-table').innerHTML=rows.length?`<table><thead><tr><th>Work Order</th><th>Asset</th><th>Type</th><th>Status</th><th>Progress</th><th>Team</th><th>Expected</th><th></th></tr></thead><tbody>${rows.map(w=>`<tr><td><strong>${escapeHtml(w.orderNumber)}</strong><span class="sub">${escapeHtml(w.customerName)}</span></td><td>${escapeHtml(w.assetCode)}</td><td>${pretty(w.workType)}</td><td><span class="badge ${badgeClass(w.status)}">${pretty(w.status)}</span></td><td>${w.progressPercent??0}%</td><td>${escapeHtml(w.assignedTeam||'—')}</td><td>${date(w.expectedCompletionAt)}</td><td><button class="mini-btn" onclick="editWork(${w.id})">Update</button></td></tr>`).join('')}</tbody></table>`:'<div class="empty">No matching work orders.</div>'}
function renderAssets(){const q=(document.getElementById('asset-search')?.value||'').toLowerCase(),f=document.getElementById('asset-filter')?.value||'';const rows=state.assets.filter(a=>(!f||a.type===f)&&[a.assetCode,a.customerName,a.currentYardLocation,a.serialNumber].join(' ').toLowerCase().includes(q));document.getElementById('asset-table').innerHTML=rows.length?rows.map(a=>`<article class="asset-card"><span class="badge ${a.type==='CONTAINER'?'blue':'green'}">${pretty(a.type)}</span><h3>${escapeHtml(a.assetCode)}</h3><span class="badge ${badgeClass(a.status)}">${pretty(a.status)}</span><div class="asset-meta"><span>Customer <strong>${escapeHtml(a.customerName)}</strong></span><span>Size ${escapeHtml(a.sizeDescription||'—')}</span><span>Location ${escapeHtml(a.currentYardLocation||'—')}</span></div></article>`).join(''):'<div class="empty">No matching assets.</div>'}
function deliveryTable(rows){return rows.length?`<table><thead><tr><th>Delivery</th><th>Asset</th><th>Vehicle</th><th>Driver</th><th>Status</th><th>ETA</th><th></th></tr></thead><tbody>${rows.map(d=>`<tr><td><strong>${escapeHtml(d.deliveryNumber)}</strong><span class="sub">${escapeHtml(d.orderNumber)}</span></td><td>${escapeHtml(d.assetCode)}</td><td>${escapeHtml(d.vehicleNumber||'—')}</td><td>${escapeHtml(d.driverName||'—')}</td><td><span class="badge ${badgeClass(d.status)}">${pretty(d.status)}</span></td><td>${date(d.expectedDeliveryAt)}</td><td><a class="mini-btn" href="/tracking.html?id=${d.id}">Track</a></td></tr>`).join('')}</tbody></table>`:'<div class="empty">No deliveries found.</div>'}
function renderDeliveries(){const q=(document.getElementById('delivery-search')?.value||'').toLowerCase();document.getElementById('delivery-table').innerHTML=deliveryTable(state.deliveries.filter(d=>[d.deliveryNumber,d.assetCode,d.vehicleNumber,d.driverName].join(' ').toLowerCase().includes(q)))}
function renderInventory(){const q=(document.getElementById('inventory-search')?.value||'').toLowerCase();const rows=state.inventory.filter(i=>[i.sku,i.name,i.preferredSupplier].join(' ').toLowerCase().includes(q));document.getElementById('inventory-table').innerHTML=rows.length?`<table><thead><tr><th>SKU</th><th>Material</th><th>On Hand</th><th>Reorder</th><th>Cost</th><th>Supplier</th><th>Status</th></tr></thead><tbody>${rows.map(i=>`<tr><td><strong>${escapeHtml(i.sku)}</strong></td><td>${escapeHtml(i.name)}</td><td>${i.quantityOnHand} ${escapeHtml(i.unit||'')}</td><td>${i.reorderLevel}</td><td>${money(i.unitCost)}</td><td>${escapeHtml(i.preferredSupplier||'—')}</td><td><span class="badge ${i.lowStock?'red':'green'}">${i.lowStock?'Low stock':'Healthy'}</span></td></tr>`).join('')}</tbody></table>`:'<div class="empty">No matching inventory items.</div>'}
function renderCustomers(){const q=(document.getElementById('customer-search')?.value||'').toLowerCase();const rows=state.customers.filter(c=>[c.name,c.companyName,c.phone,c.email].join(' ').toLowerCase().includes(q));document.getElementById('customer-table').innerHTML=rows.length?`<table><thead><tr><th>Name</th><th>Company</th><th>Phone</th><th>Email</th><th>Address</th></tr></thead><tbody>${rows.map(c=>`<tr><td><strong>${escapeHtml(c.name)}</strong></td><td>${escapeHtml(c.companyName||'—')}</td><td>${escapeHtml(c.phone||'—')}</td><td>${escapeHtml(c.email||'—')}</td><td>${escapeHtml(c.billingAddress||'—')}</td></tr>`).join('')}</tbody></table>`:'<div class="empty">No matching customers.</div>'}

function syncSelects(){document.querySelectorAll('.customer-select').forEach(s=>{const v=s.value;s.innerHTML=state.customers.map(c=>`<option value="${c.id}">${escapeHtml(c.name)}${c.companyName?' · '+escapeHtml(c.companyName):''}</option>`).join('');if(v)s.value=v});document.querySelectorAll('.asset-select').forEach(s=>{const v=s.value;s.innerHTML=state.assets.map(a=>`<option value="${a.id}">${escapeHtml(a.assetCode)} · ${pretty(a.type)}</option>`).join('');if(v)s.value=v});document.querySelectorAll('.work-select').forEach(s=>{const v=s.value;s.innerHTML=state.workOrders.map(w=>`<option value="${w.id}">${escapeHtml(w.orderNumber)} · ${escapeHtml(w.assetCode)}</option>`).join('');if(v)s.value=v})}
function formJson(form){return Object.fromEntries(new FormData(form).entries())}
function editWork(id){const w=state.workOrders.find(x=>x.id===id);if(!w)return;const f=document.getElementById('status-form');f.elements.id.value=id;f.elements.status.value=w.status;f.elements.progressPercent.value=w.progressPercent??0;f.elements.blockedReason.value=w.blockedReason||'';openModal('status-modal')}
async function submitJson(form,url,method,modal,transform=x=>x){const submit=form.querySelector('[type="submit"],button:not([type])');const old=submit?.textContent;if(submit){submit.disabled=true;submit.textContent='Saving…'}try{const data=transform(formJson(form));await api(url,{method,headers:{'Idempotency-Key':crypto.randomUUID?.()||`${Date.now()}-${Math.random()}`},body:JSON.stringify(data)});form.reset();closeModal(modal);toast('Saved successfully');await refreshAll()}catch(e){toast(e.message)}finally{if(submit){submit.disabled=false;submit.textContent=old}}}

document.getElementById('customer-form').onsubmit=e=>{e.preventDefault();submitJson(e.target,'/api/customers','POST','customer-modal')};
document.getElementById('asset-form').onsubmit=e=>{e.preventDefault();submitJson(e.target,'/api/assets','POST','asset-modal',d=>({...d,customerId:Number(d.customerId)}))};
document.getElementById('work-form').onsubmit=e=>{e.preventDefault();submitJson(e.target,'/api/work-orders','POST','work-modal',d=>({...d,customerId:Number(d.customerId),assetId:Number(d.assetId),estimatedCost:d.estimatedCost?Number(d.estimatedCost):null}))};
document.getElementById('inventory-form').onsubmit=e=>{e.preventDefault();submitJson(e.target,'/api/inventory','POST','inventory-modal',d=>({...d,quantityOnHand:Number(d.quantityOnHand),reorderLevel:Number(d.reorderLevel),unitCost:d.unitCost?Number(d.unitCost):null}))};
document.getElementById('delivery-form').onsubmit=e=>{e.preventDefault();submitJson(e.target,'/api/deliveries','POST','delivery-modal',d=>({...d,workOrderId:Number(d.workOrderId),assetId:Number(d.assetId),destinationLatitude:Number(d.destinationLatitude),destinationLongitude:Number(d.destinationLongitude)}))};
document.getElementById('status-form').onsubmit=e=>{e.preventDefault();const d=formJson(e.target),id=d.id;delete d.id;d.progressPercent=d.progressPercent?Number(d.progressPercent):null;submitJson(e.target,`/api/work-orders/${id}`,'PATCH','status-modal',()=>d)};

async function runGlobalSearch(){const q=document.getElementById('global-search').value.trim(),type=document.getElementById('global-search-type').value,status=document.getElementById('global-search-status').value.trim(),target=document.getElementById('search-results');if(!q){target.innerHTML='<div class="empty">Type a customer, asset, order, vehicle or status.</div>';return}if(searchAbort)searchAbort.abort();searchAbort=new AbortController();const params=new URLSearchParams({q,limit:'50'});if(type)params.set('type',type);if(status)params.set('status',status);target.innerHTML='<div class="skeleton" style="height:58px"></div><div class="skeleton" style="height:58px"></div><div class="skeleton" style="height:58px"></div>';try{const results=await api(`/api/search?${params}`,{signal:searchAbort.signal});document.getElementById('search-source').textContent='Resilient search';document.getElementById('search-source').className='badge green';target.innerHTML=results.length?results.map(r=>`<article class="search-result"><div class="search-result-main"><span class="badge gray">${pretty(r.type)}</span><strong>${escapeHtml(r.title||r.reference)}</strong><span>${escapeHtml(r.subtitle||'')}</span></div><div class="search-result-meta"><code>${escapeHtml(r.reference||'')}</code>${r.status?`<span class="badge ${badgeClass(r.status)}">${pretty(r.status)}</span>`:''}</div></article>`).join(''):'<div class="empty">No matching records.</div>'}catch(e){if(e.name==='AbortError')return;target.innerHTML=`<div class="empty">${escapeHtml(e.message)}</div>`;document.getElementById('search-source').textContent='Unavailable';document.getElementById('search-source').className='badge red'}}
const liveGlobalSearch=debounce(runGlobalSearch,260);
document.getElementById('global-search-btn').onclick=runGlobalSearch;document.getElementById('global-search').addEventListener('input',liveGlobalSearch);document.getElementById('global-search-type').addEventListener('change',()=>{if(document.getElementById('global-search').value.trim())runGlobalSearch()});document.getElementById('global-search-status').addEventListener('input',liveGlobalSearch);

function normalizeDependencies(health){const ft=health?.components?.faultTolerance||health?.details?.faultTolerance||health?.faultTolerance;const deps=ft?.details?.dependencies||ft?.dependencies||{};return Object.entries(deps).map(([name,value])=>({name,...value}))}
async function loadHealth(silent=false){try{const health=await api('/actuator/health');state.health=health;const overall=health.status||'UNKNOWN',deps=normalizeDependencies(health),degraded=overall==='DEGRADED'||deps.some(d=>d.state==='OPEN');document.getElementById('sidebar-status').textContent=degraded?'Degraded Mode':overall==='UP'?'System Online':pretty(overall);document.getElementById('sidebar-status-detail').textContent=degraded?'Fallbacks are active':'Dependencies monitored';document.getElementById('sidebar-status-dot').classList.toggle('warn',degraded||overall!=='UP');const banner=document.getElementById('system-banner');if(degraded){banner.classList.remove('hidden');banner.innerHTML='<strong>Graceful degradation active.</strong> Core operations remain available while one or more optional dependencies recover.'}else banner.classList.add('hidden');document.getElementById('health-overview').innerHTML=`<div class="search-result"><div class="search-result-main"><span class="badge ${badgeClass(overall)}">${pretty(overall)}</span><strong>YardFlow API</strong><span>${degraded?'Fallback policies are protecting requests.':'All monitored dependencies are healthy.'}</span></div></div>`;document.getElementById('dependency-health').innerHTML=deps.length?deps.map(d=>`<div class="dependency-card"><span class="badge ${badgeClass(d.state)}">${pretty(d.state)}</span><strong>${pretty(d.name)}</strong><span class="sub">Failures: ${d.consecutiveFailures??0} · Available permits: ${d.availablePermits??'—'}</span></div>`).join(''):'<div class="empty">No circuit has been exercised yet.</div>'}catch(e){document.getElementById('sidebar-status').textContent='Health unavailable';document.getElementById('sidebar-status-detail').textContent='API check failed';document.getElementById('sidebar-status-dot').classList.add('warn');if(!silent)toast(e.message)}}

document.getElementById('logout-btn').onclick=async()=>{try{await api('/api/auth/logout',{method:'POST'})}catch{}window.yardFlowClearSession?.();location.replace('/login.html')};
['work-search','asset-search','delivery-search','inventory-search','customer-search'].forEach(id=>{const el=document.getElementById(id);if(!el)return;const renderer={"work-search":renderWorkOrders,"asset-search":renderAssets,"delivery-search":renderDeliveries,"inventory-search":renderInventory,"customer-search":renderCustomers}[id];el.addEventListener('input',debounce(renderer,90))});document.getElementById('work-filter').onchange=renderWorkOrders;document.getElementById('asset-filter').onchange=renderAssets;

if(!loadCache())showSkeletons();const initial=(location.hash||'#dashboard').slice(1);if(document.getElementById(initial))showView(initial);refreshAll();